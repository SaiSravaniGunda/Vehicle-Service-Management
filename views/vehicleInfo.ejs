<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head')%>
    <title>ServiTrack</title>
    <!-- Add Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LAav8qGOHp/lWYWq5jpn5qIAWP/p5rj8tAHy4tNqpyIWjpAmM4vjWpL2iZ8QLKgA" crossorigin="anonymous">

    <style>
        /* Sidebar style */
        .sidebar {
            height: 100vh; /* Full height */
            width: 250px; /* Fixed width */
            position: fixed; /* Make sidebar fixed on the left */
            top: 0;
            left: 0;
            background-color: #45494e; /* Dark background */
            padding-top: 20px; /* Space at the top */
        }

        .sidebar a {
            padding: 15px 20px;
            text-decoration: none;
            font-size: 18px;
            color: white;
            display: block;
        }

        .sidebar a:hover {
            background-color: #7d7f81;
        }

        /* Main content */
        .main-content {
            margin-left: 250px; /* Same as the width of the sidebar */
            padding: 20px;
        }

        /* Fixed navbar */
        .navbar {
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
        }

        /* Adjust the main content padding due to fixed navbar */
        .main-content {
            padding-top: 60px;
        }
        @media (max-width: 768px) {
            .main-content {
                margin-left: 0; /* Remove sidebar margin for small screens */
            }
            .sidebar {
                width: 100%; /* Make sidebar full width */
                position: relative; /* Remove fixed positioning */
                height: auto; /* Allow height to adjust */
            }
        }
    </style>
</head>
<body>

    <%- include('./partials/nav')%>

    <div class="sidebar">
        <a></a>
        <a href="/vehicle/vehicles">Vehicles</a>
        <a href="/vehicle/addVehicle">Add Vehicle</a>
    </div>

    <!-- Main content -->
    <div class="main-content">
        <h2>Vehicle Details</h2>
        <p>Here are the details of your vehicle:</p>

        <!-- Vehicle details section -->
        <div class="container mt-5">
            <div class="row">
                <div class="col-md-6">
                    <div class="card text-bg-light mb-3">
                        <div class="card-header">Vehicle Information</div>
                        <div class="card-body">
                            <h5 class="card-title">Model: <span id="vehicleModel"><%= vehicle.model %></span></h5>
                            <p class="card-text">VIN: <span id="vehicleVIN"><%= vehicle.vin %></span></p>
                            <p class="card-text">Purchase Year: <span id="vehicleYear"><%= vehicle.purchaseYear %></span></p>
                            <p class="card-text">Mileage: <span id="vehicleMileage"><%= vehicle.mileage %></span></p>
                            <p class="card-text">No.of Sevices: <span id="vehicleServiceHistory"><%= vehicle.serviceHistory.length %></span></p>
                        </div>
                    </div>
                </div>
                
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>